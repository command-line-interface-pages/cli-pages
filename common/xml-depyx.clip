# xml depyx

> Convert a PYX (ESIS - ISO 8879) document to XML format
> More information: http://xmlstar.sourceforge.net/docs.php

- Convert a PYX (ESIS - ISO 8879) document to XML format:

`xml depyx {string value: path/to/input.pyx|URI} > {string value: path/to/output.xml}`

- Convert a PYX document from stdin to XML format:

`cat {string value: path/to/input.pyx} | xml depyx > {string value: path/to/output.xml}`

- Display help for the `depyx` subcommand:

`xml depyx --help`
