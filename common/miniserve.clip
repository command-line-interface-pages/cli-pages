# miniserve

> Simple HTTP file server CLI
> More information: https://github.com/svenstaro/miniserve

- Serve a directory:

`miniserve {directory value}`

- Serve a single file:

`miniserve {file value}`

- Serve a directory using HTTP basic authentication:

`miniserve --auth {string user}:{string value: password} {directory value}`
