# jarsigner

> Sign and verify Java archive (JAR) files
> More information: https://docs.oracle.com/javase/9/tools/jarsigner.htm

- Sign a JAR file:

`jarsigner {file value: sample.jar} {string value: keystore_alias}`

- Sign a JAR file with a specific algorithm:

`jarsigner -sigalg {string value: algorithm} {file value: sample.jar} {string value: keystore_alias}`

- Verify the signature of a JAR file:

`jarsigner -verify {file value: sample.jar}`
