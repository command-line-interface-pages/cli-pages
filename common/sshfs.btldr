# sshfs

> Filesystem client based on SSH
> More information: https://github.com/libfuse/sshfs

- Mount remote directory:

`sshfs {string user}@{any something}:{directory something: remote} {any something}`

- Unmount remote directory:

`umount {any something}`

- Mount remote directory from server with specific port:

`sshfs {string user}@{any something}:{directory something: remote} -p {int number: 2222}`

- Use compression:

`sshfs {string user}@{any something}:{directory something: remote} -C`

- Follow symbolic links:

`sshfs -o follow_symlinks {string user}@{any something}:{directory something: remote} {any something}`
