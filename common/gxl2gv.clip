# gxl2gv

> Convert a graph from `gxl` to `gv` format
> More information: https://graphviz.org/pdf/gxl2gv.1.pdf

- Convert a graph from `gxl` to `gv` format:

`gxl2gv -o {string value: output.gv} {string value: input.gxl}`

- Convert a graph using stdin and stdout:

`cat {string value: input.gxl} | gxl2gv > {string value: output.gv}`

- Display help:

`gxl2gv -?`
