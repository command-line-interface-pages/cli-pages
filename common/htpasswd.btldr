# htpasswd

> Create and manage htpasswd files to protect web server directories using basic authentication
> More information: https://httpd.apache.org/docs/current/programs/htpasswd.html

- Create/overwrite htpasswd file:

`htpasswd -c {file something} {any something}`

- Add user to htpasswd file or update existing user:

`htpasswd {file something} {any something}`

- Add user to htpasswd file in batch mode without an interactive password prompt (for script usage):

`htpasswd -b {file something} {any something} {any something}`

- Delete user from htpasswd file:

`htpasswd -D {file something} {any something}`

- Verify user password:

`htpasswd -v {file something} {any something}`

- Display a string with username (plain text) and password (md5):

`htpasswd -nbm {any something} {any something}`
