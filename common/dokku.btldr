# dokku

> Docker powered mini-Heroku (PaaS)
> Easily deploy multiple apps to your server in different languages using a single `git-push` command
> More information: https://github.com/dokku/dokku

- List running apps:

`dokku apps`

- Create an app:

`dokku apps:create {any something}`

- Remove an app:

`dokku apps:destroy {any something}`

- Install plugin:

`dokku plugin:install {any something}`

- Link database to an app:

`dokku {any something}:link {any something} {any something}`
