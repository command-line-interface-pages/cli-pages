# eslint

> A pluggable linting utility for JavaScript and JSX
> More information: https://eslint.org

- Create ESLint config:

`eslint --init`

- Lint on a given set of files:

`eslint {any something}.js {any something}.js`

- Fix lint issues:

`eslint --fix`

- Lint with config:

`eslint -c {file something: path/to/config} {any something}`
