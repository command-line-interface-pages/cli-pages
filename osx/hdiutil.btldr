# hdiutil

> Utility to create and manage disk images
> More information: https://ss64.com/osx/hdiutil.html

- Mount an image:

`hdiutil attach {file something: path/to/image}`

- Unmount an image:

`hdiutil detach /Volumes/{any something}`

- List mounted images:

`hdiutil info`

- Create an ISO image from the contents of a directory:

`hdiutil makehybrid -o {file something: path/to/output} {directory something}`
