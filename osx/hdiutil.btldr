# hdiutil

> Utility to create and manage disk images
> More information: https://ss64.com/osx/hdiutil.html

- Mount an image:

`hdiutil attach {file value: image}`

- Unmount an image:

`hdiutil detach /Volumes/{string value: volume_name}`

- List mounted images:

`hdiutil info`

- Create an ISO image from the contents of a directory:

`hdiutil makehybrid -o {file value: output} {directory value}`
