# shuf

> Generate random permutations
> More information: https://www.unix.com/man-page/linux/1/shuf/

- Randomize the order of lines in a file and output the result:

`shuf {file value}`

- Only output the first 5 entries of the result:

`shuf --head-count={int value: 5} {file value}`

- Write output to another file:

`shuf {file value} --output={file value: output}`

- Generate random numbers in range 1-10:

`shuf --input-range={int range: 1..10}`
