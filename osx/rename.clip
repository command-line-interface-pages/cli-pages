# rename

> Rename a file or group of files with a regular expression
> More information: https://www.manpagez.com/man/2/rename/

- Replace `from` with `to` in the filenames of a specific files:

`rename 's/{string some description: from}/{string some description: to}/' {string some description: *.txt}`
