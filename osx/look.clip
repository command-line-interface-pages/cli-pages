# look

> Look for lines in sorted file
> More information: https://manned.org/look

- Look for lines which begins with a specific prefix:

`look {string some description: prefix} {file some description}`

- Look for lines ignoring case:

`look --ignore-case {string some description: prefix} {file some description}`
