# hdiutil

> Utility to create and manage disk images
> More information: https://ss64.com/osx/hdiutil.html

- Mount an image:

`hdiutil attach {file image file}`

- Unmount an image:

`hdiutil detach /Volumes/{file volume file}`

- List mounted images:

`hdiutil info`

- Create an ISO image from the contents of a directory:

`hdiutil makehybrid -o {file output file} {directory some description}`
