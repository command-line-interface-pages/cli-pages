# logcat

> Dump a log of system messages, including stack traces when an error occurred, and information messages logged by applications
> More information: https://developer.android.com/studio/command-line/logcat

- Display system logs:

`logcat`

- Write system logs to a specific file:

`logcat -f {file output}`

- Display lines matching a specific regular expression:

`logcat --regex {string regular expression}`
