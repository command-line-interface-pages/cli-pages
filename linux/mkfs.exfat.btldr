# mkfs.exfat

> Creates an exfat filesystem inside a partition
> More information: https://manned.org/mkfs.exfat

- Create an exfat filesystem inside partition 1 on device b (`sdb1`):

`mkfs.exfat {string value: /dev/sdb1}`

- Create filesystem with a volume-name:

`mkfs.exfat -n {string value: volume_name} {string value: /dev/sdb1}`

- Create filesystem with a volume-id:

`mkfs.exfat -i {string value: volume_id} {string value: /dev/sdb1}`
