# newgrp

> Switch primary group membership
> More information: https://manned.org/newgrp

- Change user's primary group membership:

`newgrp {any something}`

- Reset primary group membership to user's default group in `/etc/passwd`:

`newgrp`
