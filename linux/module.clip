# module

> Modify a users' environment using the module command
> More information: https://lmod.readthedocs.io/en/latest/010_user.html

- Display available modules:

`module avail`

- Search for a module by name:

`module avail {string value: module_name}`

- Load a module:

`module load {string value: module_name}`

- Display loaded modules:

`module list`

- Unload a specific loaded module:

`module unload {string value: module_name}`

- Unload all loaded modules:

`module purge`
