# add-apt-repository

> Manages apt repository definitions
> More information: https://manned.org/apt-add-repository

- Add a new apt repository:

`add-apt-repository {any something}`

- Remove an apt repository:

`add-apt-repository --remove {any something}`

- Update the package cache after adding a repository:

`add-apt-repository --update {any something}`

- Allow source packages to be downloaded from the repository:

`add-apt-repository --enable-source {any something}`
