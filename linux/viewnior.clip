# viewnior

> Simple and elegant image viewer
> More information: https://manned.org/viewnior

- View an image:

`viewnior {string value: path/to/image.ext}`

- View in fullscreen mode:

`viewnior --fullscreen {string value: path/to/image.ext}`

- View fullscreen in slideshow mode:

`viewnior --slideshow {string value: path/to/image.ext}`
