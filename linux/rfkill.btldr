# rfkill

> Enable and disable wireless devices
> More information: https://manned.org/rfkill

- List devices:

`rfkill`

- Filter by columns:

`rfkill -o {string value: ID,TYPE,DEVICE}`

- Block devices by type (e.g. bluetooth, wlan):

`rfkill block {string value: bluetooth}`

- Unblock devices by type (e.g. bluetooth, wlan):

`rfkill unblock {string value: wlan}`

- Output in JSON format:

`rfkill -J`
