# choco apikey

> Manage API keys for Chocolatey sources
> More information: https://chocolatey.org/docs/commands-apikey

- List all sources and their API keys:

`choco apikey`

- Display a specific source and its API key:

`choco apikey --source "{string source url}"`

- Set a specific API key for a source:

`choco apikey --source "{string source url}" --key "{string api key}"`

- Remove an API key for a specific source:

`choco apikey --source "{string source url}" --remove`
