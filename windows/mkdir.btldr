# mkdir

> Creates a directory
> More information: https://learn.microsoft.com/windows-server/administration/windows-commands/mkdir

- Create a directory:

`mkdir {directory value}`

- Create a nested directory tree recursively:

`mkdir {directory value: sub}`
